[project]
name = "statsfactory"
version = "0.1.0"
description = "Python SDK for statsfactory anonymous event telemetry"
requires-python = ">=3.8"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"
